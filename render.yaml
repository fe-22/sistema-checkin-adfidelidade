services:
  - type: web
    name: checkin-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: seu-database-name
          property: connectionString
      - key: SECRET_KEY
        generateValue: true